
import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    historyContainer: {
        flex: 1,
        padding: 20,
        backgroundColor: '#fff', // Cor de fundo
      },
      backButton: {
        marginTop: 10,
      },
})